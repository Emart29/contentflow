# Batch Test Results

Not run yet.

Run `python scripts/batch_test.py` after configuring `.env` with a real LLM key. The script will replace this file with observed results from 5 real Single Grain blog URLs and save full outputs to `scripts/batch_results.json`.
