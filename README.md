# ContentFlow

## Demo

[Loom video link — add after recording]

Built with: LangGraph · Groq (llama-3.3-70b) · FastAPI · Tailwind CSS · Docker

Tested on 5 real Single Grain blog posts — see BATCH_TEST_RESULTS.md

## Quickstart

1. Copy `.env.example` to `.env`
2. Add your `GROQ_API_KEY`
3. Run `docker compose up --build`
4. Open `http://localhost:3000`
